export function showOptoutConfirmation() {
  let confirmer = new Promise((resolve, reject) => {
    // show dialog
    //if(true == 1) {
      // user clciks yes, resolve
      resolve();
    //} else {
    //  reject();
    //}
  });
  return confirmer;
}
